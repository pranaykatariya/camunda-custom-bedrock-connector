<?xml version="1.0" encoding="UTF-8"?>
<!--
  Same as the logback-spring.xml shipped in spring-boot-starter-camunda-connectors (which this file
  shadows), plus an opt-in JSON profile. Camunda's file uses Spring Boot's plain console appender,
  so Spring Boot's logging.structured.* switch has no effect on it.

  SPRING_PROFILES_ACTIVE=json-logs  ->  ECS JSON, including the SLF4J key/value fields this project
                                        emits (gatewayHost, httpStatus, tokenEndpoint, ...).

  Plain text: Spring Boot's default console pattern ends in %m and drops SLF4J key/value pairs, so
  the pattern below is Spring Boot's own with %kvp appended. logging.pattern.console still wins.
-->
<configuration>
  <property name="CONSOLE_LOG_PATTERN" value="${CONSOLE_LOG_PATTERN:-%clr(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd'T'HH:mm:ss.SSSXXX}}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}){} %clr(${PID:-}){magenta} %clr(--- %esb(){APPLICATION_NAME}%esb{APPLICATION_GROUP}[%15.15t] ${LOG_CORRELATION_PATTERN:-}){faint}%clr(%-40.40logger{39}){cyan} %clr(:){faint} %m %kvp%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>
  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

  <springProfile name="json-logs">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="org.springframework.boot.logging.logback.StructuredLogEncoder">
        <format>ecs</format>
        <charset>UTF-8</charset>
      </encoder>
    </appender>
  </springProfile>
  <springProfile name="!json-logs">
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
  </springProfile>

  <logger name="io.camunda" level="info"/>
  <root level="info">
    <appender-ref ref="CONSOLE"/>
  </root>
</configuration>
