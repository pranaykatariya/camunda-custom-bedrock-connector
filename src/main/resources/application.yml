CONNECTOR_AI_AGENT_TYPE: ${ORG_AI_AGENT_TASK_TYPE:org.ai-gateway:aiagent:1}
CONNECTOR_AI_AGENT_JOB_WORKER_TYPE: ${ORG_AI_AGENT_SUBPROCESS_TYPE:org.ai-gateway:aiagent-job-worker:1}

camunda:
  connector:
    agenticai:
      ad-hoc-tools-schema-resolver:
        enabled: false
      mcp:
        remote-client:
          enabled: false
      a2a:
        client:
          outbound:
            enabled: false
          polling:
            enabled: false
          webhook:
            enabled: false
          agentic:
            tool:
              enabled: false

organization:
  ai-gateway:
    auth:
      enabled: ${ORG_AI_GATEWAY_AUTH_ENABLED:true}
      token:
        header-name: ${ORG_AI_GATEWAY_TOKEN_HEADER:x-bam-token}
        header-value-template: "${ORG_AI_GATEWAY_TOKEN_HEADER_TEMPLATE:{token}}"
      static-headers:
        - name: Accept
          value: application/json
        # Placeholder: set ORG_AI_GATEWAY_HOST_HEADER to the host the gateway routes on.
        - name: Host
          value: ${ORG_AI_GATEWAY_HOST_HEADER:bedrock-gateway.placeholder.invalid}
      bam:
        token-url: ${ORG_AI_BAM_TOKEN_URL:}
        username: ${ORG_AI_BAM_USERNAME:}
        password: ${ORG_AI_BAM_PASSWORD:}

logging:
  pattern:
    # Spring Boot's default pattern ends in %m; %kvp is what emits this project's structured log fields.
    console: "%clr(%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX}){faint} %clr(%5p){} %clr(${PID:- }){magenta} %clr(--- [%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m %kvp%n%wEx"
